## 1. Scaffold the docs package

- [x] 1.1 Create `apps/docs/` and initialize it with `pnpm create astro` using the Starlight template (or manually), producing `package.json`, `astro.config.mjs`, `tsconfig.json`, and `src/`.
- [x] 1.2 Set `"name": "@tde.io/docs"` and `"private": true` in `apps/docs/package.json`. Confirm no `main`, `module`, `types`, `exports`, or `files` fields are present.
- [x] 1.3 Add `@tde.io/plexis` as a `"workspace:*"` devDependency in `apps/docs/package.json`. Add `typescript` to devDependencies for the package's build/typecheck. Do NOT add React-family deps (`@astrojs/react`, `react`, `react-dom`, `@xyflow/react`) — those belong to the deferred playground proposal.
- [x] 1.3a Add `starlight-theme-gruvbox` (`^2.0.0`) to dependencies. Ensure `@astrojs/starlight` is `^0.38.0` and `astro` is `^6.0.0` to satisfy the theme's peer dependencies.
- [x] 1.4 Add `dev` (`astro dev`) and `build` (`astro build`) scripts to `apps/docs/package.json`.
- [x] 1.5 Remove the `.gitkeep` from `apps/docs/` now that the directory has real content.

## 2. Configure Astro and Starlight

- [x] 2.1 Configure `astro.config.mjs` with `output: 'static'` and the Starlight integration (`title: 'Plexis'`, `description`). Do NOT add the React integration in this proposal.
- [x] 2.2 Define the Starlight sidebar in `astro.config.mjs` with sections: Guides (`domain`, `pipeline`, `tracer`, `error-handling`), Reference (`api`), and Contributing (`contributing/index`). No Playground entry yet.
- [x] 2.3 Configure a persistent Starlight `banner` that states the site documents the in-development ("next") version and links to the latest stable release on npm (`@tde.io/plexis`).
- [x] 2.4 Add the Gruvbox theme to the Starlight `plugins` array: `import gruvbox from 'starlight-theme-gruvbox'` and include `gruvbox()` in `plugins`.
- [x] 2.5 Confirm dark mode is enabled and that the Gruvbox light/dark variants both render via Starlight's theme toggle (Starlight default — verify `astro.config.mjs` does not disable the theme toggle).
- [x] 2.6 Remove all Starlight placeholder/example content files that were generated by the template and are not part of the planned content structure.

## 3. Update root workspace

- [x] 3.1 Add `"docs": "pnpm --filter @tde.io/docs run dev"` to the root `package.json` `scripts` block.
- [x] 3.2 Add `"docs:build": "pnpm --filter @tde.io/docs run build"` to the root `package.json` `scripts` block.
- [x] 3.3 Run `pnpm install` from the repository root to update `pnpm-lock.yaml` with `apps/docs` dependencies (including the `workspace:*` link to `@tde.io/plexis`). Confirm no unexpected dependency changes.
- [x] 3.4 Confirm `pnpm -r list --depth -1` lists `@tde.io/docs` as a workspace project.

## 4. Author documentation content

- [x] 4.1 Create `src/content/docs/index.mdx` — landing page with one-line Plexis description and Quick Start section (reuse/adapt content from `packages/plexis/README.md`).
- [x] 4.2 Create `src/content/docs/guides/domain.mdx` — Domain concept guide covering `defineDomain()`, `state()`, `edge()`, lifecycle hooks (`onEnter`/`onExit`), and `domain.follow()`. All code blocks in `typescript`.
- [x] 4.3 Create `src/content/docs/guides/pipeline.mdx` — Pipeline concept guide covering `definePipeline()`, `node()`, `fork()`, `terminal()`, and `pipeline.run()`. All code blocks in `typescript`.
- [x] 4.4 Create `src/content/docs/guides/tracer.mdx` — Tracer guide covering `createTracer()`, `TraceLevel`, and attaching a tracer to a domain.
- [x] 4.5 Create `src/content/docs/guides/error-handling.mdx` — Error handling guide covering `PlexisError`, error codes, and `ErrorPolicy`.
- [x] 4.6 Create `src/content/docs/reference/api.mdx` — API reference listing all public exports: `defineDomain`, `definePipeline`, `state`, `edge`, `node`, `fork`, `terminal`, `createTracer`, `PlexisError`, and key types. Include function signatures and one-line descriptions.
- [x] 4.7 Create `src/content/docs/contributing/index.mdx` — Contributor guide covering: cloning the repo, `pnpm install`, running tests (`pnpm test`), building the library (`pnpm build`), running examples, updating docs (`pnpm docs`), the "next"/workspace version model, and the Vercel preview deployment workflow.
- [x] 4.8 Verify all fenced code blocks across all MDX files use `typescript` or `bash` (not `ts` or `sh`).

## 5. Add the docs CI workflow

- [x] 5.1 Create `.github/workflows/docs.yml` with `on.push.branches: ['**']` trigger, `pnpm/action-setup@v4`, `actions/setup-node@v4` (node 24), `pnpm install --frozen-lockfile` at repo root, and `pnpm docs:build` (or `pnpm --filter @tde.io/docs run build`) as the build step.
- [x] 5.2 Confirm `docs.yml` has no `working-directory:` override on the install step.
- [x] 5.3 Confirm `docs.yml` has no deploy step (Vercel deployment is handled by the GitHub integration).

## 6. Configure Vercel

- [x] 6.1 Create a new Vercel project linked to the GitHub repository (via Vercel dashboard or CLI).
- [x] 6.2 In Vercel project settings, set **Root Directory** to `apps/docs` and confirm Vercel detects the pnpm workspace (so it installs from the repo root and `workspace:*` resolves).
- [ ] 6.3 Confirm Vercel detects the Astro framework automatically (or set it manually to Astro).
- [ ] 6.4 Confirm the production domain is assigned and the project deploys successfully on push to `main`.

## 7. Verify the build end to end

- [x] 7.1 From the repository root, run `pnpm install` and confirm it completes without errors.
- [x] 7.2 From the repository root, run `pnpm docs:build` and confirm the Astro build exits 0 and produces `apps/docs/dist/`. Confirm it does NOT require `pnpm build` (library) to have run first.
- [x] 7.3 From the repository root, run `pnpm build` and confirm the library build is unaffected by the docs addition.
- [x] 7.4 From the repository root, run `pnpm test` and confirm all library tests still pass.
- [x] 7.5 Locally run `pnpm docs` and open the browser. Navigate to each guide page, the API reference, and the Contributor guide. Confirm: sidebar links work, code blocks render with syntax highlighting, the "next" version banner is visible, the Gruvbox palette is applied, and the light/dark toggle switches between Gruvbox variants correctly.

## 8. Verify CI and Vercel integration

- [ ] 8.1 Push the branch and confirm the `docs.yml` workflow runs and exits 0.
- [ ] 8.2 Confirm the Vercel GitHub integration creates a preview deployment for the branch and posts a URL to the commit status.
- [ ] 8.3 Merge to `main` and confirm Vercel triggers a production deployment.
- [x] 8.4 Confirm `pnpm -r list --depth -1` includes `@tde.io/docs` in the workspace project list.
